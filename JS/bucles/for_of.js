// Bucle for...of en JavaScript
const colores = ['rojo', 'verde', 'azul'];
for (const color of colores) {
  console.log(`Color: ${color}`);
}
//for...of itera sobre los valores de una colección (como arrays o strings), permitiendo acceder directamente a cada valor.