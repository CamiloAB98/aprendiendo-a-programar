numero = input("Ingrese un número: ")# Solicita al usuario que ingrese un número

#resultado = int(numero) * 2 # Multiplica el número ingresado por 2, ponemos int() para convertir el input (que es un string) a entero

resultado = float(numero) * 2 # Multiplica el número ingresado por 2, ponemos float() para convertir el input (que es un string) a decimal

print(f"El resultado de multiplicar {numero} por 2 es: {resultado}") # Imprime el resultado de la multiplicación